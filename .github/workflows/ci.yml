name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a build command
        run: echo "Build your project here (e.g., npm run build)"

      - name: Run tests
        run: echo "Run your tests here (e.g., npm test)"
